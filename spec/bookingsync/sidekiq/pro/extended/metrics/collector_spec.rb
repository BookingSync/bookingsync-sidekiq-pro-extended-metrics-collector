RSpec.describe Bookingsync::Sidekiq::Pro::Extended::Metrics::Collector do
  it "has a version number" do
    expect(Bookingsync::Sidekiq::Pro::Extended::Metrics::Collector::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
